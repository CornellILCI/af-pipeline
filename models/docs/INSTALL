INSTALLATION

REQUIREMENTS
R version:

Packages (and dependencies)
optparse version
PBTools version
PBTools package dependencies

1. Install package dependencies: run installDeps.R
installDeps.R should contain a series of: 
"install.packages("$PATH/packages/Digger_1.0.0.tar.gz", repos=NULL, type="source")"
...
"install.packages("$PATH/packages/Optparse_2.0.0.tar.gz", repos=NULL, type="source")"

2. Install PBTools package: installPBTools.R

COPY/INSTALL designs to simba/models/statDesign folder

TESTING

make sure
name of definition file matches design R script.
name of design R script is the value of RequestMethod 
 without the "R" extension.
-o and -p parameters are the same.
name of JSON fields for parameter in request file (JSON) 
 corresponds to design R script's parameter.




